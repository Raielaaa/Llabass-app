<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Labassapp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textSize">@dimen/_10ssp</item>
        <item name="colorPrimary">@color/Theme.color.main</item>
    </style>

    <style name="Theme.Labassapp" parent="Base.Theme.Labassapp" />

    <!-- Animation for dialog -->
    <style name="animation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
    
    <!-- FloatingActionButton corner radius -->
    <style name="ShapeAppearanceOverlayExtended" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">100dp</item>
    </style>
    
    <!-- NavigationDrawer Font -->
    <style name="PoppinsTextViewStyle" parent="android:Widget.TextView">
        <item name="fontFamily">@font/poppins_bold</item>
    </style>
</resources>